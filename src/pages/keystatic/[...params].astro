---
export const prerender = false;

// Panel de administración - solo accesible en desarrollo local
// En producción redirigimos a la página principal
return Astro.redirect(
  import.meta.env.DEV ? "/keystatic/" : "/",
  import.meta.env.DEV ? 301 : 302,
);
---
